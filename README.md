# Minimal Resume Website

A single-page resume website built with plain HTML, CSS, and JavaScript animations.

## Run locally

### Option 1: Open directly

Open `index.html` in any browser.

### Option 2: Run a dev server

```bash
npm install
npm run dev
```

Then open the local URL printed in your terminal.
